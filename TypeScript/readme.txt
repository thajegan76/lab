Create a project folder
1) Open command prompt, navigate inside the project folder and type
npm init --y
2) Open command prompt, navigate inside the project folder and type
tsc --init
3) Open 2 command prompt, in the first command prompt run tsc --watch
In the second command prompt navigate to target folder and run the programs
using node program.js